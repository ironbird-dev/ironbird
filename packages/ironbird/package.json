{
  "name": "ironbird",
  "version": "0.0.0",
  "description": "Ground-test React Native apps for AI coding agents. Thin wrapper over @ironbird/cli so npx ironbird works.",
  "type": "module",
  "engines": { "node": ">=22" },
  "bin": { "ironbird": "./bin.js" },
  "files": ["bin.js"],
  "dependencies": { "@ironbird/cli": "workspace:*" }
}
