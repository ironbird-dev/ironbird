# ironbird

Ground-test React Native apps for AI coding agents. This package exposes the `ironbird` binary from `@ironbird/cli` so `npx ironbird` works without a local install. Documentation: https://github.com/ironbird-dev/ironbird
